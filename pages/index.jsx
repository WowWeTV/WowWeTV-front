import HeaderLayout from '@/component/layout/header';
import { useSelector } from 'react-redux';

const Main = () => {
  return (
    <div>
      <HeaderLayout />
    </div>
  );
};

export default Main;
